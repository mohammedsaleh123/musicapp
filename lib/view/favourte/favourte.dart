import 'package:flutter/material.dart';

class Favourte extends StatelessWidget {
  const Favourte({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
